export enum Routes {
  HOME = "/",
  MENU = "/menu",
  ABOUT = "about",
  CONTACT = "/contact",
  AUTH = "auth",
}

export enum NavLinks {
  HOMEPAGE = "home",
  MENUPAGE = "menu",
  ABOUTPAGE = "about",
  CONTACTPAGE = "contact",
  AUTHPAGE = "Login ",
}
export enum Environments {
  DEV = "development",
  PROD = "production",
}
